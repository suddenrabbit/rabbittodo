# RabbitToDo 2.0 后续迭代新对话 Prompt

复制下面整段内容到新的 Codex 对话，再在最后补充第一个新需求。

---

我们继续开发 RabbitToDo。用户名密码账户体系与旧用户加密迁移已经完成，当前从 2.0 生产基线开始下一阶段迭代。

项目仓库位于：

`/Users/zm/Documents/GitHub/rabbittodo`

生产地址：

`https://todo.srabbitwork.site`

开始工作前，请先完整阅读：

- `/Users/zm/Documents/GitHub/rabbittodo/README.md`
- `/Users/zm/Documents/GitHub/rabbittodo/DEVELOPMENT_CONVENTIONS.md`

然后检查：

```bash
git status --short
git log -5 --oneline
```

当前 2.0 基线：

- commit：`e2498f7`
- 应用版本：`v20260801.110605`
- Service Worker：`rabbittodo-v43`
- 技术栈：原生 HTML/CSS/JavaScript PWA + Cloudflare Worker + D1
- 本地预览端口：`8792`
- 本地验证：已有用户名密码账号与新用户名注册
- 账户 migration：`0006_user_accounts.sql` 已进入生产基线；后续数据库结构变化必须新增 migration。
- 生产数据库必须保留既有数据。

必须遵循以下协作约定：

1. 所有改动先在本地完成和验证，不直接修改生产数据。
2. 每次提供本地预览地址，并提醒我测试已有账号的持续登录、离线恢复和同步，同时按需测试新用户名注册。
3. 未经我明确确认，不要 commit，也不要 push。
4. 我确认 commit 时，版本号使用北京时间 `v年月日.时分秒`；秒数不必严格一致。
5. 每次提交前判断是否修改了 PWA 缓存资源。如果修改了，必须同步更新 `APP_VERSION`、`EXPECTED_SERVICE_WORKER_VERSION` 和 `public/sw.js` 的缓存版本。
6. 文档或纯后端改动不机械更新 Service Worker。
7. 数据库结构变化必须新增 D1 migration，先本地验证；生产发布时先执行 migration，再部署依赖新字段的代码。
8. migration 必须保护生产既有数据，不能删除或重建生产任务表。
9. 生产既有用户均已完成升级，旧 6 位身份码入口保持关闭；不能再用 1.0 Worker 回退这些账号。
10. 开始修改前先复述你对需求的理解；涉及产品逻辑或数据模型的大调整，先讨论并等我确认。
11. 不要重新引入 `SERVER_MASTER_KEY` 或其他额外服务器主密钥；修改密码和管理员重置不得改变内部身份码或任务密文。
12. Worker 端 PBKDF2 密码哈希固定为 100,000 次，不能超过 Cloudflare Workers 上限；浏览器端任务密钥派生为 120,000 次，两者用途不同。
13. D1 免费套餐单次调用存在查询限制，老用户原子升级当前最多处理 40 条任务；调整该上限前必须重新核算并验证完整事务。

当前重要背景：

- 任务分为待办和已办，支持详情、颜色、多标签、可选计划日期、进行中、暂停、完成、置顶和拖拽排序。
- 待办支持置顶区与普通区，拖入/拖出自动切换置顶；已办记录完成时间。
- 2.0 已使用用户名、密码和 HttpOnly 会话；新用户名确认一次密码后自动注册，不再审批，管理员仍可启用、禁用账号并生成 15 分钟有效的一次性密码重置码。
- 已登录设备持续解锁并将任务快照和离线操作保存在 IndexedDB；启动先读本地再拉取远端。新增和编辑每次都直接尝试远端，不得以 `navigator.onLine` 等网络状态字段跳过请求；仅超时或网络层失败进入 outbox 并标记“未同步”。遗留 outbox 不阻塞远端读取，需合并保留本机未同步项并在前台恢复、网络状态变化和定时检查时持续重试，成功后清除标记；会话闲置 180 天过期，活跃使用自动续期，多个设备可同时登录。
- 密码只用于认证；改密或重置密码不改变内部身份码和任务密文。标题、详情保持浏览器端 AES-GCM 加密，标签及其他业务字段保持明文。
- 管理后台位于 `/console/`，默认管理员密码继续以哈希回退形式保留；账户体系不依赖额外服务器主密钥。
- PWA 启动先显示本地快照，再检查 Service Worker 更新；检测到新版仅显示“立即更新”，确认后持久化本地操作、激活新版并刷新，远程同步在更新检查后执行。
- 竖屏固定顶部标题与筛选区、横屏固定左侧功能区；两种方向均仅滚动任务列表。自动同步和同一列表内重新渲染必须保留当前滚动锚点，横屏保持双栏布局。
- 生产发布曾暴露本地 Miniflare 未限制 PBKDF2 迭代数的问题。涉及 Cloudflare 运行时能力时必须核对平台限制，不能只依据本地测试通过。

请先确认你已理解项目现状和开发约定，不要立即改代码。我的第一个新需求是：

【在这里填写下一版本的第一个需求】

---
